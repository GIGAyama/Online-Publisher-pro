# オンライン出版社 Pro 操作マニュアル

このマニュアルは、実際のアプリの画面と動作にそって書かれた操作手順書です。
導入（デプロイ）の方法は [README.md](README.md) を参照してください。

## 目次

- [0. 画面の共通ルール](#0-画面の共通ルール)
- [1. 児童編：作品を書いて提出する](#1-児童編作品を書いて提出する)
- [2. 先生編：提出物を見て添削する](#2-先生編提出物を見て添削する)
- [3. みんなの作品だな：読んで感想を送る](#3-みんなの作品だな読んで感想を送る)
- [4. 管理編：パスワードとAPIキー](#4-管理編パスワードとapiキー)
- [5. データの保存場所と管理](#5-データの保存場所と管理)
- [6. 困ったときは](#6-困ったときは)
- [7. アプリとしてインストールする（PWA）](#7-アプリとしてインストールするpwa)
- [付録A ステータス一覧](#付録a-ステータス一覧)
- [付録B ショートカットキー一覧](#付録b-ショートカットキー一覧)
- [付録C 挿絵の形一覧](#付録c-挿絵の形一覧)

---

## 0. 画面の共通ルール

### 3つのモード

画面いちばん上のバーで、3つのモードを切り替えます。モードによってバーの色が変わります。

| モード | 切り替え方 | 色 | ログイン |
| --- | --- | --- | --- |
| 児童モード（かく） | 左上のロゴ「オンライン出版社 Pro」をクリック | オレンジ | 不要 |
| みんなの作品だな | 右上の「みんなの作品」ボタン | 緑 | 不要 |
| 先生用 | 右上の「先生用」ボタン | 赤（ローズ） | **パスワードが必要** |

### 歯車（設定）ボタン

上部バーの歯車ボタンは、**今いるモードによって開く内容が変わります**。

- 児童モード・ギャラリーで押した場合 → 原稿用紙の**用紙設定**（文字数・禁則処理）
- 先生モードで押した場合 → **システム設定**（ログインパスワードの変更、Gemini APIキーの設定）

### ふりがな

児童が使う画面の漢字には、ふりがな（ルビ）が振られています。

---

## 1. 児童編：作品を書いて提出する

児童モードは左右2つのエリアに分かれています。

- **左：エディタ**（「書く」「挿絵」の2タブ）
- **右：プレビュー**（原稿用紙／本）

> スマートフォンやタブレットの縦画面では左右が並ばず、いちばん上の「**かく**」「**プレビュー**」タブで切り替えて、それぞれ全画面で使います。

### 1-1. 文章を書く

1. 「書く」タブを開きます。
2. **だいめい** / **がくねん・くみ** / **なまえ** を入力します。
3. 大きな入力欄に本文を書きます。
4. 入力欄の右下に、**改行を除いた文字数**が表示されます。

書いた内容は右側のプレビューにすぐ反映されます。エディタをスクロールすると、プレビューの原稿用紙も連動してスクロールします。

「基本情報」の行に出るバッジで、いまの作品の状態が分かります。

| バッジ | 意味 |
| --- | --- |
| グレー「下書き」 | まだ提出していません。自由に書けます |
| 青「提出済」 | 先生に提出済み。**本文も挿絵も編集できません** |
| オレンジ「再提出」 | 先生から直しを求められて返ってきました。**もう一度編集できます** |
| 緑「完了」 | 先生が完成と認めました。**編集できません** |
| 🔒「提出済のため編集できません」 | 入力欄がグレーになっている理由の説明です |
| ℹ️「変更あり」 | 先生が最後に添削したときから本文が変わっています。先生の画面にも印が付きます |

### 1-2. 保存のしくみ（保存ボタンはありません）

このアプリに「保存ボタン」はありません。次のタイミングで自動的に保存されます。

| タイミング | 動作 |
| --- | --- |
| 入力が **20秒** 止まったとき | サーバー（スプレッドシート）へ自動保存 |
| タブを閉じる・別アプリに切り替える・画面を消したとき | その場ですぐ自動保存 |
| 「提出する」を押したとき | 保存してから提出 |

本文の入力欄の下に、いまの保存状態が表示されます。

| 表示 | 意味 |
| --- | --- |
| ✏️ 編集中 | まだ保存されていない変更があります（20秒待つと保存されます） |
| 🔄 保存中... | サーバーへ送信しています |
| ☁️ クラウド保存済 | サーバーへの保存が完了しました |
| 🚫 オフライン (端末に一時保存) | ネットにつながっていません。内容は端末に残っており、**つながり次第、自動で送信されます** |
| ⚠️ エラー (端末に一時保存) | 送信に失敗しました。内容は端末に残っています |

> **注意**：「提出済」「完了」になった作品は編集がロックされるため、自動保存も行われません。

**アプリを閉じても、書きかけは消えません。** 最後に書いていた内容はその端末のブラウザにも
控えられていて、次に同じ端末で開いたときに自動でその続きが表示されます。
別の端末で続きを書くときは、「**開く**」から作品を選び直してください。

### 1-3. 挿絵を入れる

1. 左のエリアで「**挿絵**」タブを開きます。
2. 「画像を選択して追加」の枠を押し、端末の画像を選びます。
   - 画像は端末側で**幅800px以下のJPEG**に縮小してからGoogleドライブへアップロードされます。
3. 追加された挿絵カードで、次の設定ができます。

| 項目 | 内容 |
| --- | --- |
| 配置ページ | 「本」モードの何ページ目に置くかを数字で指定します（**1ページ目は表紙**です） |
| 大きさ | 30%〜150%のスライダー |
| 切り抜き | 8種類の形から選びます（[付録C](#付録c-挿絵の形一覧)） |
| ▲ / ▼ | 同じページに複数置いたときの並び順を入れ替えます |
| 🗑 | その挿絵を削除します |

> **挿絵が表示されるのは「本」モードだけです。** 原稿用紙モードには挿絵は出ません。
> 同じページに挿絵を置くと、そのページの本文は1行の文字数が約6割に減り、挿絵の下に収まるように組まれます。

### 1-4. プレビューを切り替える

プレビュー右上のボタンで切り替えます。

- **原稿用紙**：縦書きのマス目表示。1行目に題名（上から4マス目から）、次の行に学年組と名前が下寄せで入ります。
- **本**：1ページ目が表紙（題名・学年組・名前）、2ページ目からが本文（35字×13行）。挿絵はページ上部に並びます。

#### 用紙の設定（歯車ボタン）

| 設定 | 選べる値 |
| --- | --- |
| 用紙の文字数 | **20文字×20行（高学年）** / **15文字×16行（低学年）** |
| 禁則処理 | **追い出し**（行末の禁則文字を次の行へ送る） / **ぶら下がり**（マスの外に小さく表示する） |

この設定は使っている端末のブラウザに保存され、次回も引き継がれます。

### 1-5. 印刷する

プレビュー右上の「**印刷**」ボタンを押します。用紙は **A4横** です。

- 「**原稿用紙**」を表示中 → 原稿用紙のレイアウト（中央に綴じ代と題名・ページ番号）で1ページずつ出力
- 「**本**」を表示中 → 見開き2ページを1枚に並べて出力

### 1-6. 新しく書く／保存した作品を開く

- **新規**：今の内容を消して、まっさらな状態から書き始めます。書きかけの内容があるときは確認画面が出ます。
- **開く**：サーバーに保存されている作品の一覧から選んで開きます。題名・学年組・名前・ステータス・更新日時が表示されます。

> 「開く」の一覧には、**そのアプリに保存されている全員分の作品**が表示されます。自分の名前を目印に探してください。

### 1-7. 提出する

「**提出する**」ボタンを押すと、先生の一覧に「提出済」として届きます。

提出後は本文・挿絵とも**編集できなくなり**、ボタンが「**コピーして新しく書く**」に変わります。これを押すと挿絵ごと複製した新しい下書きが作られ、先生の添削や感想は引き継がずに書き直せます。

> **提出はネットにつながっているときだけできます。** オフラインで押すと
> 「サーバーと通信できませんでした」と出て、提出されません。
> （書いた内容は端末に残っているので、つながってからもう一度押してください。
> 下書きの自動保存はオフラインでも働きます。）

### 1-8. 先生の添削を読む

先生が添削すると、原稿用紙プレビューの該当する文字に**赤いマーク**が付きます。

1. 赤いマークの文字をタップ（クリック）します。
2. 先生からのコメントが表示されます。
3. 直し終えたら「**修正完了（赤線を消す）**」を押します。その赤線は児童の画面から消えます（先生の画面には残ります）。

> 本文を書き直しても、添削の位置は本文の変化に合わせて自動で追従します。

### 1-9. 先生の操作のお知らせ

「**提出済**」の作品を開いている間、アプリは **1分おき** にサーバーを確認しています。先生が操作すると、次のメッセージが表示され、画面が自動で最新の内容に更新されます。

| 先生の操作 | 表示されるメッセージ |
| --- | --- |
| 「完了」にした | 先生が作品を「完了」にしました！ よくがんばりました！ |
| 「再提出」にした | 先生から「再提出」で返却されました。コメントを確認して直してみましょう。 |
| 添削・コメントを更新した | 先生が添削・コメントを更新しました。確認してみましょう。 |

「再提出」で返された作品は**もう一度編集できる状態**に戻ります。直したあとに「提出する」を押すと内容と添削の状態が保存されます（一覧上のステータスは「再提出」のままです）。

---

## 2. 先生編：提出物を見て添削する

### 2-1. ログイン

1. 右上の「**先生用**」ボタンを押します。
2. パスワードを入力します（**初回は `admin`**）。
3. ログインすると提出一覧が読み込まれます。

> ログイン状態はページを開いている間だけ保持されます。再読み込みすると再度ログインが必要です。

### 2-2. 提出一覧（左側）

一覧には**下書き以外**（提出済・再提出・完了）の作品が、更新日時の新しい順に並びます。

| 機能 | 説明 |
| --- | --- |
| キーワード検索 | 名前・題名・本文を対象に絞り込みます |
| ステータス | 完了以外（初期値）／すべて表示／提出済／再提出／完了 |
| 日付 | 更新日の開始日〜終了日で絞り込みます |
| ↻ ボタン | 最新のデータを取り直します（`U` キーでも可） |
| ? ボタン | ショートカット一覧を開きます |

各行には児童の名前・ステータスバッジ・題名が並びます。前回の添削時から本文が変わっている作品には、青い**差分アイコン**が付きます。

行の左のチェックボックスは**一括操作の対象**を選ぶためのものです。

> 画面が狭い端末では、一覧と添削画面が切り替え表示になります。作品を選ぶと添削画面が開き、左上の「←」で一覧に戻ります。

### 2-3. 添削画面（右側）

上部のバーには、選択中の作品の学年組・名前・題名と、次のボタンが並びます。

| ボタン | 説明 |
| --- | --- |
| 原稿 / 本 / 差分 | プレビューの切り替え（`V` キーで順に切り替え） |
| 印刷 | 表示中の作品を印刷（`P` キー） |
| AI添削 | Geminiに添削案を作らせる（「原稿」表示中のみ表示。`A` キー） |
| 再提出 | ステータスを「再提出」にする（`R` キー） |
| 完了 | ステータスを「完了」にする（`C` キー） |

作品を選ぶと、本文が前回の添削時から変わっている場合は自動で「**差分**」表示、それ以外は「**原稿**」表示で開きます。

### 2-4. 赤線（添削）を付ける／消す

**付ける**

1. プレビューを「**原稿**」表示にします。
2. 原稿用紙の上で、指摘したい文字を**ドラッグ**して範囲を選びます。
   - タブレットなどのタッチ端末では、**文字を長押ししてから、そのままなぞって**選択します（短いスワイプはスクロールになります）。
3. 指を離すとコメント入力欄が開くので、修正指示やアドバイスを入力し「追加する」を押します。

**消す**

赤いマークの付いた文字をクリックすると確認画面が開き、「削除する」でその添削を消せます。

> 児童が「修正完了」にした添削は、児童の画面では消えますが、先生の画面には**緑色のマーク**で残ります。

### 2-5. AI添削を使う

事前に[システム設定](#4-管理編パスワードとapiキー)でGemini APIキーを登録しておく必要があります。

- **1件だけ**：「原稿」表示で「**AI添削**」ボタン（または `A` キー）。
- **まとめて**：一覧でチェックを入れ、一覧上部の 🪄 ボタン（または `Shift + A`）。確認画面で「一括添削を開始」を押します。件数が多いと数分かかります。

AIは「小学校の先生」として、誤字脱字・表現の改善点・良い点を、小学生に伝わる言葉で指摘します。指摘は本文中の該当箇所に `【AI】` 付きの赤線として追加され、そのまま保存されます。

> AIが引用した文字列が本文中に見つからなかった指摘は、赤線として追加されません。そのため「◯件のAI提案を追加しました」の件数がAIの指摘数より少なくなることがあります。
> AIの指摘はあくまで下書きです。不要なものはクリックして削除し、先生の言葉で書き換えてから児童に返してください。

### 2-6. 差分表示

「**差分**」タブでは、**前回添削したときの本文**と**現在の本文**を比較して表示します。

- 緑のハイライト：追加された部分
- 赤の取り消し線：削除された部分

再提出された作品のどこが直ったかを確認するのに使います。

### 2-7. 一括操作

一覧でチェックを入れてから、一覧上部のボタンを押します。

| ボタン | 動作 | ショートカット |
| --- | --- | --- |
| 🪄 | 選択した作品を一括AI添削 | `Shift + A` |
| ✅ | 選択した作品を一括で「完了」にする | — |
| 🖨 | 選択した作品を一括印刷 | `Shift + P` |

一括印刷のレイアウトは、そのとき表示しているプレビューに従います（「原稿」表示中なら原稿用紙、「本」「差分」表示中なら本のレイアウト）。

### 2-8. 印刷

「印刷」ボタンを押すとブラウザの印刷ダイアログが開きます。**用紙はA4横**を選んでください。ヘッダー・フッターはオフを推奨します。

---

## 3. みんなの作品だな：読んで感想を送る

1. 上部バーの「**みんなの作品**」を押します。
2. 「提出済」または「完了」になっている作品が本棚に並びます。感想が付いている作品には、右上に件数のバッジが出ます。
3. 読みたい本を選ぶと、全画面のブックリーダーが開きます。

### ページのめくり方

| 操作 | 動作 |
| --- | --- |
| `←` キー / 画面左の矢印 / 左へスワイプ | 次のページ |
| `→` キー / 画面右の矢印 / 右へスワイプ | 前のページ |
| `Esc` キー / 左上の「一覧にもどる」 | 一覧に戻る |

縦書きの本なので、**右から左へ**めくっていきます。

> 画面の左右に出る大きな矢印は、横幅が広い画面（PC・Chromebook）のときだけ出ます。
> タブレットやスマートフォンでは、本の下に「◀ 3 / 8 ▶」のようなページ送りが出ます。

### 感想を送る

画面右側（縦画面では下側）の「感想・コメント」欄で、

1. 「あなたのなまえ」を入力します（空欄のまま送ると「名無し」になります）。
2. 「かんそうをかこう」に感想を書き、送信ボタンか `Enter` キーを押します。

送った感想はすぐ一覧に追加され、スプレッドシートの「交流コメントデータ」シートに記録されます。

> 感想の削除・編集はアプリからはできません。不適切な投稿があった場合は、スプレッドシートの「交流コメントデータ」シートの行を直接削除してください。

---

## 4. 管理編：パスワードとAPIキー

先生モードでログインした状態で、上部バーの**歯車ボタン**を押すと「システム設定」が開きます。この画面は児童モードには表示されません。

### ログインパスワードの変更

「新しいパスワードを入力」に入力して「保存して閉じる」を押します。**初期パスワード `admin` は必ず変更してください。**

> GASエディタ → プロジェクト設定 → スクリプトプロパティ の `TEACHER_PASSWORD` を直接書き換えても変更できます。

### Gemini APIキーの設定

1. [Google AI Studio](https://aistudio.google.com/) でAPIキーを取得します。
2. 「AI添削用APIキー」に貼り付けて「保存して閉じる」を押します。

キーはスクリプトプロパティ `GEMINI_API_KEY` に保存され、AI添削のときにサーバー側から使われます。ブラウザ側には保存されません。

> パスワード変更・APIキー設定・AI添削・先生用の一覧取得は、いずれもサーバー側で教師パスワードを検証しています。児童側の画面からこれらの操作を実行することはできません。

---

## 5. データの保存場所と管理

初回アクセス時に、デプロイした先生のマイドライブに次のファイルが自動生成されます。手動で削除しても、次のアクセス時に作り直されます（中のデータは戻りません）。

### スプレッドシート `オンライン出版社Pro_データベース`

**シート「作文データ」**

| 列 | 内容 |
| --- | --- |
| 作品ID | 自動採番されるUUID |
| 題名 / 学年・クラス / 氏名 / 本文 | 児童が入力した内容 |
| ステータス | `draft` / `submitted` / `rework` / `completed` |
| 挿絵データ | 挿絵の一覧（JSON） |
| 添削データ | 添削時点の本文と赤線の一覧（JSON） |
| 先生コメント | 予備の列（現在のアプリ画面からは入力しません） |
| 作成日時 / 更新日時 | 日時 |
| 削除日時 | **値を入れるとアプリ上に表示されなくなります** |

**シート「交流コメントデータ」**

コメントID / 作品ID / 投稿者名 / コメント本文 / 投稿日時

### Driveフォルダ `オンライン出版社Pro_画像データ`

挿絵の画像ファイルが入ります。アプリの `<img>` から表示するため、フォルダは「**リンクを知っている全員が閲覧可**」に設定されます。個人が特定できる写真の扱いには注意してください。

### 作品を消したいとき

アプリに削除機能はありません。スプレッドシート「作文データ」の該当行の「**削除日時**」列に日付などの値を入力すると、その作品は児童・先生・ギャラリーのどの画面にも表示されなくなります（行そのものは残ります）。完全に消す場合は行ごと削除してください。

---

## 6. 困ったときは

| 症状 | 確認すること |
| --- | --- |
| 「オフライン (端末に一時保存)」と出たまま | ネットワーク接続を確認してください。**タブを閉じずに**接続が戻れば自動で送信されます |
| 児童が書いた内容が消えた | まず**同じ端末・同じブラウザでアプリを開き直して**ください。最後に書いていた内容がそのまま復元されます。それでも無いときは「開く」から同じ題名・名前の作品を探してください（**入力が20秒止まったときの自動保存**まで戻せます） |
| 提出したのに先生の一覧に出ない | 先生画面で「↻（更新）」または `U` キーを押してください。絞り込みが「完了」などになっていないかも確認します |
| 「提出する」を押すと「サーバーと通信できませんでした」と出る | オフラインです。提出には通信が必要です。書いた内容は端末に残っているので、つながってから押し直してください |
| 直したはずの文が元に戻っている | 同じ作品を2つの端末（タブ）で開いていませんか。あとから保存したほうで上書きされます。1つの作品は1つの端末で開いてください |
| 先生用パスワードが分からない | GASエディタ → プロジェクト設定 → スクリプトプロパティ の `TEACHER_PASSWORD` で確認・変更できます |
| AI添削で「Gemini APIキーを設定してください」と出る | システム設定でAPIキーを登録してください |
| AI添削が「AI添削の実行に失敗しました」で止まる | APIキーが有効か、利用上限に達していないかを確認してください |
| 挿絵が表示されない | 「本」モードで表示していますか（原稿用紙モードには挿絵は出ません）。配置ページ番号が本文のページ数を超えていないかも確認します |
| 印刷がはみ出す | 用紙を **A4・横向き**、余白を「なし」、ヘッダーとフッターをオフにしてください |
| タブレットで添削の範囲選択ができない | 文字を**長押ししてから**なぞってください。短いスワイプはスクロール操作になります |
| ショートカットキーが効かない | 検索欄などの入力欄からカーソルを外してください |
| PWAシェルでURLを入れ直したい | アプリ右下の「URLを変える」を押してください |
| インストールしたアプリが古いまま | 画面の下に「あたらしい バージョンが あります」と出たら「さいしんに する」を押してください。出ないときは、一度アプリを閉じてから開き直します |
| 圏外で開いたら「いま インターネットに つながっていません」と出た | 正常な動作です。書いたぶんは端末に残っています。Wi-Fi がつながってから「もういちど ためす」を押してください |
| 「インストール」ボタンが出ない | すでにインストール済みか、iPad・iPhone です。iPad・iPhone は下の §7 の手順で追加してください |
| iPad で画面の下のボタンが押しにくい | ホームバーに重ならないよう修正済みです。それでも押しにくい場合は、アプリを一度閉じて開き直してください |

---

## 7. アプリとしてインストールする（PWA）

ブラウザのタブではなく、**独立したアプリの窓**として起動できるようにする手順です。
毎回URLを開く必要がなくなり、圏外でも「つながっていません」の画面が正しく出ます。

インストールするのは、先生から配られた **PWAシェルのURL**（`https://～/pwa/` で終わるアドレス）です。
GASのウェブアプリのURL（`/exec` で終わるもの）ではありません。

### 7-1. Chromebook・Windows・Mac（Chrome / Edge）

1. PWAシェルのURLを開きます。
2. 画面の中の緑色の **「📲 アプリとしてインストール」** を押します。
   （アドレスバー右側のインストールアイコンからでも同じです）
3. 「インストール」を押すと、シェルフ（タスクバー）にアイコンが追加されます。

> ボタンが出ないときは、すでにインストール済みです。シェルフを確認してください。

### 7-2. iPad・iPhone（Safari）

iPad・iPhone には「インストール」ボタンが出ません。Safari の機能で追加します。
**Chrome ではなく Safari で開いてください。**

1. Safari で PWAシェルのURLを開きます。
2. 画面下（iPad は画面上）の **共有ボタン**（□に↑の矢印）を押します。
3. メニューを下にたどって **「ホーム画面に追加」** を押します。
4. 名前を確認して **「追加」** を押します。ホーム画面にアイコンが増えます。

> **⚠️ iPad・iPhone をお使いの学級への注意**
> Safari は、7日間まったく開かなかったサイトの保存データを自動で消す仕様があります。
> ホーム画面に追加しておくと消えにくくなりますが、**大事な作品は「提出」まで済ませて
> スプレッドシート側に保存する**運用にしてください。書きかけのまま長期休業に入ると
> 端末側の下書きが消えることがあります。

### 7-3. Androidタブレット（Chrome）

1. Chrome で PWAシェルのURLを開きます。
2. 画面の中の **「📲 アプリとしてインストール」**、またはメニュー（⋮）→「ホーム画面に追加」→「インストール」を押します。

### 7-4. 更新のしかた

アプリを新しくしたときは、児童の画面の下に
**「あたらしい バージョンが あります」** と出ます。
**「さいしんに する」** を押すと最新版になります。押さなければそのまま使い続けられるので、
授業の途中で勝手に画面が入れ替わることはありません。

### 7-5. 印刷の前に確認すること

印刷は端末の設定に左右されます。**はじめて印刷する前に、必ずプレビューで確認してください。**

1. 児童画面（または先生画面）の **「印刷」** を押します。
2. ブラウザの印刷プレビューが開きます。ここで次の4点を設定します。
   - **用紙サイズ：A4**
   - **向き：横**
   - **余白：なし**
   - **「背景のグラフィック」にチェックを入れる**
3. プレビューに **原稿用紙のマス目** と、添削の **赤い線** が出ていることを確かめます。
   - マス目が出ない → 「背景のグラフィック」のチェックが外れています。
   - 右端が切れる → 「余白」が「なし」になっていません。
4. 問題なければ印刷します。

> 一度この設定をすると、同じ端末では次回から記憶されます。
> 学級で使う前に、先生の端末と児童機1台で確かめておくと安心です。

---

## 付録A ステータス一覧

| ステータス | バッジの色 | 意味 | 児童の編集 | ギャラリー掲載 |
| --- | --- | --- | --- | --- |
| 下書き (`draft`) | グレー | 書きかけ。先生の一覧には出ません | できる | されない |
| 提出済 (`submitted`) | 青 | 先生に提出済み | できない（コピーは可） | される |
| 再提出 (`rework`) | オレンジ | 先生が直しを求めて返却した状態 | できる | されない |
| 完了 (`completed`) | 緑 | 先生が完成と認めた状態 | できない（コピーは可） | される |

## 付録B ショートカットキー一覧

先生用画面で使えます（`?` キーで画面内にも表示されます）。入力欄にカーソルがあるときは動作しません。

| キー | 動作 |
| --- | --- |
| `↑` / `↓` | 児童（作品）の切り替え |
| `V` | プレビュー切り替え（原稿用紙 → 本 → 差分） |
| `S` | 表示中の作品を一括選択 / 解除 |
| `U` | データの更新 |
| `A` | 表示中の作品のAI添削 |
| `Shift + A` | 選択した作品の一括AI添削 |
| `P` | 表示中作品の印刷 |
| `Shift + P` | 選択した作品の一括印刷 |
| `C` | 完了にする |
| `R` | 再提出にする |
| `?` | ショートカット一覧の表示 / 非表示 |
| `Esc` | 開いているモーダルを閉じる |

ギャラリーのブックリーダーでは `←`（次のページ）／`→`（前のページ）／`Esc`（閉じる）が使えます。

## 付録C 挿絵の形一覧

| 表示名 | 見た目 |
| --- | --- |
| しかく（ひょうじゅん） | 角の少し丸い長方形。画像全体が入ります |
| かどまる | 角を大きく丸めた長方形。画像全体が入ります |
| まる | 正円に切り抜きます |
| ふわふわ | ゆがんだ円形（アメーバ状）に切り抜きます |
| ひしがた | ひし形に切り抜きます |
| ほしがた | 星形に切り抜きます |
| はっかくけい | 八角形に切り抜きます |
| チケット | 四隅を斜めに落としたチケット風に切り抜きます |

※「しかく」「かどまる」以外は正方形に切り抜かれるため、写真の端が切れることがあります。
