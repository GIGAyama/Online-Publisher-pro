# ROLLOUT — GIGA Standard v4 適用の記録

| リポジトリ | 型 | P0 | P1(表示/PWA) | P2 | P3 | ゲート | 備考 |
|---|---|:--:|:--:|:--:|:--:|:--:|---|
| Online-Publisher-pro | C+ | ✅ | ✅ | ✅ | ✅ | ✅ | 第1群。PWAシェルは `pwa/` にあり、GAS本体は iframe 内 |

## このリポジトリで実際に何が壊れていたか

同じ構成の他リポジトリに展開するとき、**まず疑うべき順**に並べてある。

1. **manifest に `id` が無い**（`start_url` が暗黙の識別子になっていた）
   `github.io` は全リポジトリが同一オリジンを共有するため、URLを少し直しただけで
   別アプリ扱いになったり、似た構成の別アプリと取り違えられる。
   → `id` は**これまでの暗黙の値と同じ絶対パス**を明示すること。値を変えると
   すでにインストール済みの端末で別アプリになる。
2. **`beforeinstallprompt` の受け取りが body 末尾**
   Chrome は条件が揃うと即座に合図を出す。通信の遅い端末で取りこぼす。
   → `<head>` の最上部、`<link rel="manifest">` より前へ。
3. **インストールボタンが初回画面にしか無い**
   2回目以降に開いた児童は初回画面を通らないため、永久にインストールできない。
4. **`cache.addAll()`**
   1本の 404 で precache 全体が落ち、オフライン起動できなくなる。
   → 1本ずつ `cache.add()` して失敗は握りつぶす。
5. **`viewport-fit=cover` を付けているのに safe-area-inset が無い**
   iPad のホームバーに操作部が潜り込む。cover を付けたら必ずセットで足す。
6. **印刷CSSに `print-color-adjust: exact` が無い**
   原稿用紙のマス目・添削の赤線が「背景」と見なされ、既定の設定では印刷されない。
   作文・ワークシート系では致命的。
7. **`flex` の `justify-content: center` + `overflow-y: auto`**
   内容が画面より高いとき、上端がはみ出したままスクロールで戻せない。
   iPhone SE の横向きやソフトキーボード表示時に出る。
   → `justify-content` を外し、先頭/末尾の子に `margin: auto` を使う。
8. **アイコンPNGが可逆圧縮のかけ直しで 1/3 になる**
   画質を落とさずに済むので、まずロスレス再圧縮を試すこと。
   このリポジトリでは 98KB → 31.6KB（画素は完全一致）。

## 検証のやりかた（GAS本体を含むリポジトリ向け）

GAS の `index.html` は `google.script.run` が無いとブラウザで動かないため、
そのままでは画面を確認できない。次の手順でローカル検証できる。

1. `index.html` をコピーし、`google.script.run` を返り値固定のスタブに差し替える。
2. 外部CDN（React / Babel / Tailwind）が閉域網で落ちる場合は、npm から
   同じものを取ってきて `vendor/` に置き、`<script src>` を書き換える。
3. `npx http-server . -p 8000` で配信し、Chromium（Playwright）で
   320 / 375 / 810 / 1366px の各幅を描画して確認する。
   - 横スクロールの有無
   - 44px 未満のタップ領域
   - 名前の無い（aria-label も文字も無い）ボタン
   - コンソールのエラー
4. **改修前のコミットでも同じ手順を回し、差分として比べる。**
   「元から出ていた表示」を新しい不具合と誤認しないために必須。

## 次にやること（このリポジトリ）

優先順。いずれも AUDIT.md に対処方針を書いてある。

1. **CDN依存の解消**（初回転送量 約1.5MB → 1/4 以下）。Babel standalone の撤去だけで約900KB減る。
   これが終わってはじめて CSP を投入できる。
2. **OAuth スコープの確認**（`auth/drive` 全体が付いている可能性）。要・先生の操作、要・再認可。
3. **提示モード**（ギャラリーを電子黒板に映す用途）。UIの追加になるため合意が必要。
